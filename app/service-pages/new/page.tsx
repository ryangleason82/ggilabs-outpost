import Link from "next/link"; import { ServicePageImportForm } from "@/components/ServicePageImportForm";
export default function NewServicePage() { return <div className="mx-auto max-w-4xl p-8"><Link href="/service-pages" className="text-sm text-zinc-600">← Service pages</Link><h1 className="mt-4 text-2xl font-semibold">Import service pages</h1><p className="mb-6 mt-1 text-sm text-zinc-600">Upload a Content Engine CSV containing one complete WordPress-safe page in each <code>html_content</code> field. Outpost validates the entire file before creating drafts.</p><ServicePageImportForm/></div>; }
