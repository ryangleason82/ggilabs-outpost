import Link from "next/link"; import { notFound } from "next/navigation"; import { getSelectedClientId } from "@/lib/clients"; import { servicePageForClient, serializeServicePage } from "@/lib/service-page-data"; import { ServicePageEditor } from "@/components/ServicePageEditor";
export default async function ServicePageDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const page = await servicePageForClient(id, await getSelectedClientId()); if (!page) notFound(); const serialized = serializeServicePage(page); if (!serialized.currentRevision) notFound(); return <div className="p-8"><Link href="/service-pages" className="text-sm text-zinc-600">← Service pages</Link><h1 className="mb-5 mt-3 text-2xl font-semibold">{page.title}</h1><ServicePageEditor page={JSON.parse(JSON.stringify(serialized))}/></div>; }
